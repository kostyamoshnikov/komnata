# КОМНАТА СВЕТА — сайт

**Версия: `komnata-v10`**

Статический сайт театральной компании «Комната света».
Домен: **komnata-sveta.ru** (GitHub Pages) — целевой, но пока НЕ
подключён, сайт живёт на `kostyamoshnikov.github.io/komnata/`. Файл
`CNAME` сознательно убран из репозитория с `komnata-v9` (см. журнал) —
если он есть, GitHub Pages сразу редиректит github.io-адрес на
`komnata-sveta.ru`, а тот пока указывает на старый сайт. Возвращать
`CNAME` — только вместе с переключением DNS у регистратора, не раньше.

Чистый HTML/CSS/JS без сборки — что лежит в репозитории, то и деплоится.

## ⚠️ Главное архитектурное отличие от Аэлиты

У Аэлиты часть CSS общая (`assets/style.css`), часть — локальная в каждой
странице, и там своя головная боль с каскадом (см. её README).

**У Комнаты общего стиля нет вообще** — `assets/` содержит только
`analytics-events.js` и `img/`, никакого `style.css`. **Каждая страница —
полностью самодостаточный `<style>` внутри своего `index.html`**, без
исключений (это не три страницы, как у Аэлиты, — это вообще все страницы).

Следствие: у Комнаты нет риска Аэлиты «правка в общем файле неожиданно
поменяла вид на 5 других страницах». Зато есть **обратный** риск —
**правка в одной странице никак не попадает на остальные сама**. Уже
наступали на это: адаптив под маленькие телефоны (`≤430px`, `≤375px`) был
на `index.html`, а на 12 остальных страницах отсутствовал полностью, пока
не перенесли вручную в каждую (см. журнал изменений).

**Правило:** если правишь что-то, что концептуально должно быть на всех
страницах (брейкпоинт, hover-эффект, шапка, футер, cookie-баннер,
mobile-меню) — правь **на всех затронутых страницах разом**, и держи
список того, что уже проверено/перенесено, а что нет. Одной правки на
одной странице недостаточно, даже если кажется, что «остальные такие же».

## ⚠️ Правило при любой правке стилей

Перед тем как считать правку CSS законченной — обязательно:

1. **Проверить баланс скобок** в каждом затронутом файле (`{` = `}`) —
   одна лишняя/недостающая ломает всё, что идёт после неё в файле.
2. **Проверить остальные страницы того же типа** — если правишь что-то в
   `about/index.html`, а концептуально это должно быть и в `team/`,
   `partners/`, `projects/` и т.д. — не считать задачу законченной, пока
   не перенесено везде, где нужно (см. раздел выше).
3. **Задокументировать в этом README** — что изменилось, почему, и на
   каких именно страницах (полный список, не «и остальные похожие»).

Если правка мелкая и точечная — пункт 3 может быть одной строкой в
соответствующем разделе. Но пропускать его не надо.

## ⚠️ Правило при добавлении/удалении страницы

Если появилась новая страница (новая папка + `index.html`) или старая
удалена — сразу же:

1. Добавь/убери её в `sitemap.xml`, **кроме**: `404.html` (никогда не
   было и не должно быть) и `oferta`/`privacy`/`cookies`/`bot` — они
   закрыты в `robots.txt` и намеренно не в sitemap.
2. Добавь/убери её в `robots.txt`, если это ещё одна юридическая/служебная
   страница, которую не нужно индексировать.
3. Проверь, что на неё есть хотя бы одна ссылка с сайта (нав-меню, футер
   или связанная страница) — иначе физически существует, но никто не
   найдёт кроме как по прямому адресу.
4. Скопируй `<style>` с одной из существующих страниц того же типа
   (обычная / юридическая / карточка спектакля) как основу — не собирай
   с нуля, и сразу перенеси актуальный набор брейкпоинтов под телефоны
   (см. журнал изменений, `komnata-v1`).

Быстрая проверка после правки — сверить список папок сайта со списком
`<loc>` в `sitemap.xml`: на `komnata-v1` совпадают один в один (минус
`bot`/`cookies`/`oferta`/`privacy`, которые закрыты в `robots.txt` — это
осознанно, не баг).

## ⚠️ Версия

Каждое обновление сайта — новый номер версии в формате `komnata-vN`
(строка в самом верху этого файла). При любой правке, после которой
собирается новый архив на выдачу:

1. Увеличить номер на 1.
2. Сообщить номер отдельно, явно — не прятать в середине ответа.

## Структура

```
/
├── index.html                главная
├── <раздел>/index.html       каждая страница — своя папка + index.html
│                             (даёт чистые адреса вида /viy, /tickets)
├── archive/                  хаб + подстраницы архива (papa, raskol,
│                             gorshok, ostrov-chehova — по спектаклю)
├── assets/analytics-events.js, assets/img/, assets/docs/   общие ассеты
│                             (НЕ style.css; docs/ — PDF 4 юр. страниц)
├── favicon.ico
├── robots.txt, sitemap.xml
└── 404.html                  самодостаточна, как и все остальные страницы
```

## Дизайн-токены

```css
--void:  #050505   фон, «чёрная комната»
--ink:   #0F0F0F   поверхности, карточки
--steel: #8C8C8C   серый скос портала — вторичный текст, обводки
--mist:  #D9D9D9   светло-серый — приглушённые подписи
--light: #FFFFFF   белый проём — основной текст, «свет»
```

Заголовки — `'Playfair Display'`, капитель, `letter-spacing:.15–.2em`.
Текст — `'Inter'`, вес 300–400. Подключены через Google Fonts — офлайн-шрифт
не захостить локально не пытались, сайт зависит от интернета на загрузке.

### `:root{--maxw}`

Как и у Аэлиты — переменная разная для двух групп страниц:
- `1320px` — обычные страницы (по умолчанию)
- `900px` — юридические (`oferta`, `privacy`, `cookies`, `documents`, `bot`)

На `komnata-v1` проверено — соблюдается на всех текущих страницах, без
исключений.

## Юридические страницы и индексация

`robots.txt` закрывает `/oferta`, `/privacy`, `/cookies`, `/bot` — у
Комнаты их на одну больше, чем у Аэлиты (там `/bot` индексируется). Это
осознанное решение для Комнаты, не баг и не расхождение с Аэлитой по
недосмотру — если решение изменится, не забудь синхронно поправить и
`robots.txt`, и `sitemap.xml`.

## Плавающая нижняя плашка (`.tg-widget`)

С `komnata-v2` виджет стоит на всех 27 страницах сайта (был только на
главной на `komnata-v1`). В отличие от Аэлиты — запас снизу в футере под
него отдельно не закладывался (на `index.html` его и не было изначально,
перенесли как есть). Если в футере на маленьких экранах начнёт наезжать
на реквизиты — см. подход Аэлиты: запас нужно считать на **раскрытый
бабл** (`.tg-bubble.visible`), а не просто на кнопку — бабл делает виджет
примерно вдвое выше на несколько секунд после скролла.

## Изображения

Проще, чем у Аэлиты: все локальные изображения — в `assets/img/`, единый
источник, никакого Cloudinary/Tilda/сторонних доменов пока не
используется. `loading="lazy"` расставлен не везде (сейчас есть только на
`index`, `bufest`, `butusov`, `team`) — стоит проверить и добавить на
остальных страницах с картинками, кроме первого/hero-изображения.

Telegram-бот компании — `@komnata_sveta_bot`, отдельный канал компании —
`t.me/lightroom_theatre`. Страница `/bot` на сайте — только описание
условий использования бота, с самим ботом кодом не связана.

## Контакты в футере

Кряжева Светлана Вячеславовна (творческий проект «Комната света»)
lightroom.theatre@gmail.com · t.me/lightroom_theatre
С `komnata-v7` плашки-заглушки ИНН и адреса для корреспонденции убраны с
юридических страниц (`bot`, `oferta`, `cookies`, `privacy`) — блок
реквизитов на всех четырёх сведён к имени и e-mail, вслед за финальными
PDF-версиями этих документов (см. журнал изменений). Реквизиты (ИНН,
адрес) по-прежнему не заполнены — если/когда появятся, их нужно будет
не просто вписать вместо плашки, а сначала добавить в актуальные PDF
(они теперь источник истины по тексту этих 4 страниц), и только потом
синхронизировать на сайт.

## Аналитика и домен

**Домен ещё не подключён, и с `komnata-v9` в репозитории больше нет
файла `CNAME`** (см. журнал изменений — раньше он там лежал заранее и
это оказалось ошибкой: GitHub Pages редиректит github.io-адрес на
указанный в `CNAME` домен сразу, не дожидаясь готовности DNS у
регистратора). Когда DNS будет настроен и домен готов принимать
трафик — тогда же, синхронно, вернуть `CNAME` с `komnata-sveta.ru` в
корень репозитория, не раньше.

**GA4 — сознательно не подключаем.** Раньше в `assets/analytics-events.js`
код был готов слать события и в Метрику, и в GA4 (`gtag`) — ветка `gtag`
убрана, отслеживается только Метрика.

**Яндекс.Метрика — подключить после того, как домен заработает.**
Счётчик пока не зарегистрирован (`YM_COUNTER_ID = 0` в
`assets/analytics-events.js` — заглушка). Когда домен подключат, порядок
действий:

1. Зарегистрировать счётчик на `metrika.yandex.ru` для `komnata-sveta.ru`,
   получить числовой ID счётчика.
2. Вставить сниппет счётчика в `<head>` **всех 27 страниц** (сейчас его
   нет вообще ни на одной) — шаблон:
   ```html
   <script type="text/javascript">
      (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
      m[i].l=1*new Date();
      for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
      k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
      (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");
      ym(ВАШ_ID, "init", {ssr:true, webvisor:true, clickmap:true, ecommerce:"dataLayer", accurateTrackBounce:true, trackLinks:true});
   </script>
   <noscript><div><img src="https://mc.yandex.ru/watch/ВАШ_ID" style="position:absolute;left:-9999px" alt=""/></div></noscript>
   ```
3. Заменить `ВАШ_ID` на реальный, и в этом сниппете, и в
   `YM_COUNTER_ID` внутри `assets/analytics-events.js`.

Пункты 2–3 — на 27 страницах вручную не разъедутся, если сделать
скриптом (по аналогии с тем, как переносили `.tg-widget` в `komnata-v2`) —
дай знать, когда будет ID счётчика, сделаю разом по всем файлам.

## SEO — статус аудита

Полный чек-лист (по образцу `aelita-v7`) пройден в `komnata-v5` по всем 27
страницам — title/description длина, дубли, `og:image`/`twitter:image`,
`h1`, `alt`, `canonical`==`og:url`. Найденное описано в журнале
`komnata-v5`, включая один отложенный TODO — обновление галереи команды на
домене `komnata-sveta.ru` (см. пункт 7 в `komnata-v5`).

- `theme-color` — есть на всех 27 страницах.
- `<link rel="canonical">` и `<loc>` в `sitemap.xml` — единообразны (оба
  без слэша на конце).
- Вес изображений — не критично, есть возможность ужать несколько
  крупных PNG (см. `komnata-v5`, п.8), не блокирует ничего.

## Журнал изменений

### `komnata-v10` — адаптив под маленькие телефоны докатан ещё на 9 страниц

Баг-репорт (скриншот): на `/team` хлебная крошка «Комната Света / Команда»
и надпись «КОМАНДА» стоят впритык к левому краю на телефоне. Причина —
`/team` не попала в список из 13 страниц, куда адаптив под `≤430px`/
`≤375px` (сужение `.wrap`-паддинга, уменьшение `h1`/`h2`/`.eyebrow`/лого)
раскатали ещё в `komnata-v1`, и с тех пор так и не попала — та же дыра, о
которой предупреждали в `v1`/`v2`/`v5` про архив, просто в этот раз для
самого набора брейкпоинтов, а не для `.tg-widget` или SEO-полей.

Проверил все 27 страниц по факту наличия `max-width:430px` в файле —
кроме `/team` без него оказались ещё 8: `/projects`, `/erevan`,
`/collaboration`, `/partners`, `/archive` (хаб) и все 4 подстраницы
архива (`gorshok`, `papa`, `ostrov-chehova`, `raskol`). Добавлен один и
тот же блок (три `@media`, `600–431px`/`430px`/`375px`) на все 9 — с
поправкой на разметку: у `/team` заголовки не голые `h1`/`h2`, а
`.show-hero h1`/`.sec-head h2` (своя структура страницы), у остальных 8 —
как и везде, обычные `h1`/`h2`. Значения (`clamp()`, паддинги,
letter-spacing) — те же, что на всех уже покрытых страницах, ничего не
подбирал заново.

Баланс `{`/`}` проверен на всех 10 файлах после правки — сходится.

⚠️ **На будущее:** список из 13 страниц в `v1` был неполным ещё тогда —
на сайте уже было больше 13 страниц на момент той правки. Если появится
ещё одна новая страница — брать этот блок с любой уже покрытой (например,
`about`) как есть, а не полагаться на память о том, что «раньше вроде бы
везде раскатали».

### `komnata-v9` — `CNAME` убран (преждевременный редирект на старый домен)

Баг-репорт: открытие `kostyamoshnikov.github.io/komnata/` уводило на
`komnata-sveta.ru` — а туда пока рано, домен ещё резолвится на старый
сайт (Tilda, см. TODO про `/team` в `komnata-v5`), не на этот репозиторий.

Причина — файл `CNAME` (добавлен в `komnata-v4` как задел на будущее,
с расчётом «пока просто лежит, подключим DNS отдельно»). На практике так
не работает: GitHub Pages сам по факту наличия `CNAME` в репозитории
редиректит github.io-адрес на указанный домен, независимо от того,
настроены ли A/CNAME-записи у регистратора. DNS не был готов — итог:
единственный публичный адрес сайта вёл на чужой контент.

**Исправление:** файл `CNAME` удалён из корня репозитория. Сайт снова
доступен по `kostyamoshnikov.github.io/komnata/` напрямую, без редиректа.
Ничего в HTML/CSS/JS не менялось — только удалён один файл.

⚠️ **На будущее:** `CNAME` возвращать в репозиторий только в связке с
настройкой DNS у регистратора, одним действием — не заранее «про запас»,
как это было сделано в `v4`. Это же учесть и для фото на `/team` (см.
`komnata-v5`, п.7) — они тоже завязаны на момент переключения домена.

### `komnata-v8` — кнопка «Скачать PDF» на 4 юридических страницах

Текст на страницах **не менялся** — только добавлена возможность скачать
финальный PDF того же документа.

1. **PDF-файлы добавлены в `assets/docs/`** — `oferta.pdf`, `cookies.pdf`,
   `privacy.pdf`, `bot-rules.pdf`. Это те же финальные версии, с которыми
   сверяли текст в `komnata-v7` — при следующей правке текста на любой из
   4 страниц не забыть обновить и соответствующий PDF в `assets/docs/`,
   иначе они разойдутся так же, как расходились до `v7`.
2. **Кнопка `.pdf-btn` добавлена на `oferta`, `cookies`, `privacy`, `bot`**
   — в конце `.doc-body`, рядом с существующей `← На главную`, обе внутри
   новой обёртки `.doc-actions` (flex, с переносом на узких экранах).
   Стиль и разметка одинаковы на всех 4 страницах, добавлены в `<style>`
   и HTML каждой страницы отдельно (см. «Главное архитектурное отличие»
   выше — общего CSS-файла нет). Атрибут `download` со своим именем файла
   на каждой странице (например, `oferta-komnata-sveta.pdf`), чтобы при
   скачивании файл не назывался просто `oferta.pdf`.
3. Баланс `{`/`}` проверен на всех 4 файлах после правки — сходится.
4. На `documents/`, `about/` и остальные 23 страницы кнопка не добавлялась
   — там нет соответствующего юридического текста, скачивать нечего.

Файлы: `oferta/index.html`, `cookies/index.html`, `privacy/index.html`,
`bot/index.html`, плюс новые `assets/docs/*.pdf`.

### `komnata-v7` — сверка текста 4 юридических страниц с финальными PDF

Появились финальные PDF-версии (`Public-Offer`, `Cookies`, `Bot-Rules`,
`Privacy` `-komnata-sveta-final.pdf`) — сверил текст на сайте с ними
построчно. Основной текст (все пронумерованные пункты) совпадал слово в
слово везде, кроме перечисленного ниже. **PDF считается источником
истины по тексту этих 4 страниц** — расхождения правились в сторону PDF.

1. **Домен-плейсхолдер заполнен** на `oferta`/`cookies`/`privacy` —
   было `<span class="placeholder">[указать домен сайта]</span>`, стало
   `komnata-sveta.ru` (в PDF домен уже указан явно).
2. **Google Analytics убран из текста** `cookies` и `privacy` — упоминался
   в двух местах на каждой странице («аналитические cookie», «сторонние
   cookie» / «категории данных», п.5.3), хотя PDF, `README` (`komnata-v4`)
   и сам `assets/analytics-events.js` уже давно сходятся на том, что GA4
   сознательно не используется. Это был не баг синхронизации между
   страницами, а забытый старый текст на конкретно этих двух страницах.
   Заодно поправлена грамматика — с «сервисы»/«их» на «сервис»/«этого»,
   раз остался один.
3. **`https://` убран перед `komnata-sveta.ru`** в двух местах на `bot`
   (п.1.3, п.9.1) — нигде больше на сайте домен в тексте (не в `href`) не
   пишется с протоколом, PDF тоже без него.
4. **Блок реквизитов приведён к виду из PDF на всех 4 страницах:**
   плашки-заглушки `ИНН: [указать при наличии]` и
   `Адрес для корреспонденции: [указать]` (плюс `Банковские реквизиты:
   [указать при наличии]` — только на `oferta`) убраны — в финальных PDF
   их уже нет, остались только имя и e-mail. На `bot` дополнительно
   вернул из PDF две строки, которых на сайте не было вообще: описание
   роли («Оператор Telegram-бота @komnata_sveta_bot творческого проекта
   «Комната света»») и отдельную строку `Telegram: t.me/komnata_sveta_bot`.
   ⚠️ Реквизиты по-прежнему не заполнены по существу (см. «Контакты в
   футере» выше) — это про формат отображения незаполненного состояния,
   не про то, что ИНН/адрес появились.

Файлы: `bot/index.html`, `oferta/index.html`, `cookies/index.html`,
`privacy/index.html`. CSS/JS/структура не затронуты — правка только
текста внутри `.doc-body`, балансы скобок не проверял за ненадобностью
(стилей не трогал).

### `komnata-v6` — доп. сверка по устройствам с актуальной Аэлитой

Сравнил полный список `@media`-правил на обеих сайтах (не только ширину
экрана, но и `hover`/`pointer`, `orientation`, `prefers-color-scheme`,
`print`, `safe-area`/`viewport-fit`, retina-ресолюшн) — у Аэлиты ничего
из этого не используется, значит переносить нечего, кроме одного пункта:

1. **`prefers-reduced-motion:reduce` добавлен на 5 юридических страницах**
   (`bot`, `cookies`, `oferta`, `privacy`, `documents`) — на остальных 23
   страницах правило уже было, а на этих пяти отсутствовало, хотя у них
   тоже есть CSS-переходы (hover на ссылках и т.п.). Без этого правила
   пользователи с системной настройкой «уменьшить движение» получали бы
   анимации именно там, где обычно ждут максимально спокойный, читаемый
   текст — на страницах оферты и политики. Правило то же самое, что и на
   остальном сайте: `*{animation:none!important;transition:none!important}`
   + `scroll-behavior:auto`.
2. Брейкпоинты `480px`/`420px` у Аэлиты — не системный «уровень
   устройства», а точечные правки под конкретные компоненты (например,
   `.socials-grid` в 1 колонку на `/contacts`). У Комнаты `420px` уже
   используется по той же логике (28 вхождений) — переносить отдельно
   нечего, это не пропущенный брейкпоинт, а нормальная практика точечных
   правок по месту.

### `komnata-v5` — полный SEO-чек-лист (по образцу `aelita-v7`)

Прогон по всем **27** страницам (в прошлый раз при точечной проверке
`komnata-v3` 4 подстраницы архива не попали в выборку — та же дыра, что
уже была с `.tg-widget` в `komnata-v2`; держим в голове на будущее: всегда
проверять полный список из `sitemap.xml`, не только страницы первого
уровня).

1. **Title/description за пределами нормы — только на подстраницах
   архива**, остальные 23 страницы уже были в норме (проверено в
   `komnata-v3`):
   - `/archive/gorshok` — title 89→55, description 181→129
   - `/archive/ostrov-chehova` — title 76→47, description 165→114
   - `/archive/papa` — title 68→47 (description был в норме)
   - `/archive/raskol` — title 70→49, description 190→136
   
   Полные названия спектаклей (`«Иван Ильич и Алёша Горшок»` и т.д.) не
   сокращались — резалась только сопроводительная часть.
2. **og:image/twitter:image — теперь единообразны на всех 27**, включая
   4 подстраницы архива (у них уже стояло верно, не трогали).
3. **Дублей title/description нет** — проверено программно по всем 27
   страницам.
4. **По одному `<h1>` на странице — везде**, проверено по всем 27.
5. **`alt` — 100% покрытие**, включая подстраницы архива (не проверялись
   отдельно раньше).
6. **`canonical` == `og:url`** — сверено выборочно, расхождений нет.
7. **⚠️ Найдено, отложено на потом: 6 фото команды на `/team` захардкожены
   на `https://komnata-sveta.ru/files/block/...`** — это абсолютные ссылки
   на сам домен `komnata-sveta.ru`, а не на `assets/img/`. Домен пока не
   подключён к этому репозиторию (см. «Аналитика и домен» выше) — если
   сейчас `komnata-sveta.ru` резолвится на старый сайт (Tilda, судя по
   пути `/files/block/`), фото на `/team` работают только пока жив старый
   сайт. Как только домен переключат на этот репозиторий (GitHub Pages),
   пути `/files/block/...` перестанут существовать — 6 фото на `/team`
   разом станут битыми.
   **TODO (сделать отдельным заходом, не сейчас):** обновить галерею
   команды — скачать актуальные фото (6 шт, могут потребоваться свежие,
   не обязательно те же самые) и загрузить в `assets/img/` вместо
   хардкода на `komnata-sveta.ru`. До этого момента `/team` остаётся
   зависимым от старого сайта — учитывать при планировании переключения
   DNS на GitHub Pages: сначала обновить фото, потом переключать домен,
   не наоборот.
8. Крупные PNG в `assets/img/` (`logo-icon-light-hero.png` 86 КБ,
   `logo-full-original-og.png` 85 КБ, `logo-icon-light-hero-v2.png` 72 КБ,
   `logo-full-light-sm.png` 70 КБ) — не критично, но при желании ужать
   можно (WebP/сжатие), не блокирует ничего.

### `komnata-v4`

1. **`CNAME` создан** — `komnata-sveta.ru`, в корне репозитория. Со
   стороны GitHub Pages готово; DNS-записи у регистратора домена — вне
   репозитория, отдельный шаг.
2. **GA4 исключён из планов.** Ветка `gtag` убрана из
   `assets/analytics-events.js` — решение не подключать Google Analytics,
   не техническая недоделка.
3. **Яндекс.Метрика — задокументирован порядок подключения**, но не
   выполнено: решили сделать после того, как заработает домен. Раздел
   «Аналитика и домен» выше — пошаговая инструкция на будущее (счётчик,
   вставка сниппета на все 27 страниц, замена `YM_COUNTER_ID`).

### `komnata-v3`

1. **`twitter:image`/`twitter:card` добавлены на `archive`, `partners`,
   `team`.** У них уже был `og:image`, но не было тега `twitter:card` —
   значит превью в Twitter/X использовало бы дефолтный мелкий формат
   вместо `summary_large_image`. Приведено к тому же виду, что на
   остальных страницах (тот же файл `logo-full-original-og.png`).
2. **Title/description сокращены до безопасной длины:**
   - `/bufest` — title 67→53 симв., description 163→154.
   - `/erevan` — title 75→48 симв., description 211→149.
   - `/zavist` — description 199→160 (title был в норме, не трогали).
   `og:title`/`og:description` на этих страницах уже были отдельными и
   короче — не менялись, только `<title>` и `<meta name="description">`.
3. **`/bot`, `/cookies`, `/documents`, `/oferta`, `/privacy` — решено
   оставить без `og:image`/`twitter:image`.** Не недосмотр: страницы
   закрыты в `robots.txt` от индексации и не предполагают шаринг в
   соцсетях, превью для них не нужно.

### `komnata-v2`

1. **`.tg-widget` перенесён на все страницы.** Раньше был только на
   `index.html` (см. `komnata-v1`). Решение — расширить на весь сайт.
   Перенесён на все 26 остальных страниц, включая 4 подстраницы архива
   (`archive/gorshok`, `archive/ostrov-chehova`, `archive/papa`,
   `archive/raskol`) — их не было в первоначальном списке из 22, нашлись
   при переносе. Разметка, CSS и JS — один в один с `index.html`, с
   поправкой на глубину относительного пути до `assets/` (`../` для
   страниц первого уровня, `../../` для подстраниц архива).
   Баланс скобок проверен на всех 27 файлах.
2. **`loading="lazy"` — пересмотрено, изменений не потребовалось.** При
   ближайшем рассмотрении на страницах без lazy-загрузки (`about`,
   `archive`, `cherry`, `viy` и т.д.) единственные `<img>` — это три
   повторения маленькой иконки лого (в шапке, футере и мобильном меню),
   не контентные фото. На `bufest`/`butusov`/`team`, где есть реальные
   фотографии (Бутусов, команда), `loading="lazy"` уже стоит корректно.
   Ставить `lazy` на лого в шапке было бы не только бесполезно, но и
   контрпродуктивно — это элемент над сгибом. Пункт закрыт без правок.

### `komnata-v1`

1. **Адаптив под маленькие телефоны перенесён на все страницы.** До этой
   версии полный набор точных брейкпоинтов (`600–431px`, `430px`, `375px`
   — уменьшение h1/h2, `.wrap`-паддинга, `.eyebrow`, лого в шапке,
   паддинга секций, мобильного меню) был **только на `index.html`**, а
   `375px` не было даже там. Перенесено на все 13 страниц, существовавших
   на тот момент (`about`, `bufest`, `butusov`, `cherry`, `contacts`,
   `cookies`, `documents`, `oferta`, `privacy`, `seagull`, `tickets`,
   `viy`), плюс `index.html`. Точечно: `bufest`/`tickets` (есть
   `.ev-date`) и `butusov` (есть `.bio-photo`) получили доп. сжатие этих
   элементов на `430`/`375px`.
2. **Страница `/bot` создана.** Скопирована структура с `/cookies`
   (юр.страницы у Комнаты однотипны), контент — 9 разделов правил
   использования `@komnata_sveta_bot`, адаптированных под физлицо
   (Кряжева Светлана Вячеславовна) вместо ООО. Title/description написаны
   с нуля под тему бота — важно: у Аэлиты на `/bot` был баг (title
   скопирован со страницы cookies), в Комнату этот баг сознательно **не**
   перенесён. Пункт «Правила использования бота» в хабе `/documents`
   переведён из статуса «скоро» в рабочую ссылку.
3. **README создан** — адаптирован из README Аэлиты, с поправкой на то,
   что архитектура CSS у Комнаты противоположная (см. раздел про главное
   отличие выше), а не идентичная.

С версии `komnata-v1` начинается ведение этого файла — более раннюю
историю правок (до создания README) в явном виде не восстановить, только
то, что сохранилось в переписке.
